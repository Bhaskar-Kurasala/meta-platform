# Infrastructure

Infrastructure as Code for the platform.

## Structure

- `terraform/` - Terraform configs (modules, environments)
- `kubernetes/` - K8s manifests (base, overlays)
- `docker/` - Dockerfiles
- `migrations/` - Database migrations
- `scripts/` - Infra scripts
