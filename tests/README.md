# Tests

Test suites for the platform.

## Structure

- `unit/` - Unit tests (per component)
- `integration/` - Integration tests (flows, API)
- `e2e/` - End-to-end tests (user-journeys, smoke-tests)
- `fixtures/` - Test data (mocks, seeds)
