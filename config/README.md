# Configuration

Configuration files for the platform.

## Structure

- `defaults/` - Default configs (gateway, orchestrator, memory-service, agents)
- `environments/` - Environment overrides (dev, staging, prod)
