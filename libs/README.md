# Shared Libraries

Shared libraries used across all services.

## Structure

- `contracts/` - Schemas & validation (events, API, JSON schemas)
- `grpc/` - gRPC definitions (memory, orchestrator, tool-gateway, agent-runtime)
- `utils/` - Shared utilities (logger, metrics, tracing)
- `config/` - Shared config types
